const state = {
}

const mutations = {
}

const getters = {
}

const actions = {
    changeLocale(_, locale){
        console.log('setLocale')
    }
}

export default {
    state,
    getters,
    mutations,
    actions
}
