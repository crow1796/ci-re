const state = {
    sites: []
}

const mutations = {}

const getters = {
    sites: (context) => context.sites
}

const actions = {
}

export default {
    state,
    getters,
    mutations,
    actions
}
