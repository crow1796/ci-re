const state = {
    notes: []
}

const mutations = {}

const getters = {
    notes: (context) => context.notes
}

const actions = {}

export default {
    state,
    getters,
    mutations,
    actions
}
